server:
  port: 8185
  tomcat:
    basedir: E://temp
spring:
  application:
    name: provider
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true